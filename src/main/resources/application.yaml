spring:
  cloud:
    kubernetes:
      secrets:
        enabled: true
        fail-fast: true
        paths: /etc/secrets

management:
  endpoint:
    health:
      probes:
        enabled: true
  health:
    livenessState:
      enabled: true
    readinessState:
      enabled: true
  server:
    port: 8081
egvp:
  client:
    testUserId:
    baseUri:
